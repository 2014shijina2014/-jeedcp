#\u751F\u4EA7\u73AF\u5883

server.name=http://localhost:8083/cas

##
# Unique CAS node name
# host.name is used to generate unique Service Ticket IDs and SAMLArtifacts.  This is usually set to the specific
# hostname of the machine running the CAS node, but it could be any label so long as it is unique in the cluster.
host.name=localhost:8083


# == Basic database connection pool configuration ==
database.driverClass=oracle.jdbc.driver.OracleDriver

database.url=jdbc:oracle:thin:@192.168.2.58:1522:utf8
database.user=security2
database.password=security2
