#\u5F00\u53D1\u73AF\u5883

#server.name=http://192.168.2.178/cas
server.name=http://localhost:8080/cas

##
# Unique CAS node name
# host.name is used to generate unique Service Ticket IDs and SAMLArtifacts.  This is usually set to the specific
# hostname of the machine running the CAS node, but it could be any label so long as it is unique in the cluster.
#host.name=192.168.2.178
host.name=localhost:8080


#mysql database setting
database.type=mysql
database.driverClass=com.mysql.jdbc.Driver
database.url=jdbc:mysql://localhost:3306/jeesite?useUnicode=true&characterEncoding=utf-8
database.user=root
database.password=root


